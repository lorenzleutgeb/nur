#!/bin/bash
TUNNEL_PORT_MIN=50000
TUNNEL_PORT_MAX=60000
TUNNEL_PORT_DIFF=$(($TUNNEL_PORT_MAX-$TUNNEL_PORT_MIN))
TUNNEL_PORT=$(($(($RANDOM%$TUNNEL_PORT_DIFF))+$TUNNEL_PORT_MIN))
ssh -fNT -R $TUNNEL_PORT:localhost:$TUNNEL_PORT leutgeb.xyz && echo "$TUNNEL_PORT"
